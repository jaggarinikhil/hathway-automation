# Hathway STB Renewal Automation — Python Dependencies
# Install with: pip install -r requirements.txt

# Browser automation
playwright>=1.42.0

# OCR
pytesseract>=0.3.10
Pillow>=10.0.0

# Utilities (usually bundled with Python but listed for clarity)
# asyncio  — built-in
# pathlib  — built-in
# dataclasses — built-in (3.7+)
